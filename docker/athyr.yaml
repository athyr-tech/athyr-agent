server:
  grpc_port: 9090
  http_port: 8080

storage:
  data_dir: /app/data

llm:
  backends:
    - name: openrouter
      type: openrouter
      url: https://openrouter.ai/api/v1
      api_key: ${OPENROUTER_API_KEY}
